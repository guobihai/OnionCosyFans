<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="cc.onion.cosyfans">

    <application
        android:name=".UApplication"
        android:allowBackup="true"
        android:icon="@mipmap/cosyfans_icon"
        android:label="@string/app_name_main"
        android:roundIcon="@mipmap/ic_launcher_cosyfans"
        android:supportsRtl="true"
        android:theme="@style/ConsyFansActivityTheme"
        tools:replace="android:label"
        tools:ignore="GoogleAppIndexingWarning"
        >
        <!--启动页-->
        <activity
            android:name=".SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/ConsyFansActivityTheme.FullScreen"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--屏幕适配方案-->
        <meta-data
            android:name="design_width_in_dp"
            android:value="375" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="667" />
    </application>


</manifest>